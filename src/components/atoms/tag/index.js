import styled from '@emotion/styled'

export default styled.div(({ palette }) => ({
  margin: '.5rem',
  padding: '.5rem',
  fontSize: '.7rem',
  background: `${palette.blue} !important`,
  color: 'white'
}))
